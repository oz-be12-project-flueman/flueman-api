# 개발/테스트 전용(운영에 필요 없음)

# =================================
# Lint / Format / Type Checking
# =================================
ruff==0.6.9            # 린트 + 포맷(ruff-format) 통합
mypy==1.11.2           # 정적 타입 체크

# =================================
# Test
# =================================
pytest==8.3.3          # 단위/통합 테스트 러너
pytest-cov==5.0.0      # 커버리지 리포트

# =================================
# Git Hooks / Commit Style
# =================================
pre-commit==3.8.0      # Git 훅 프레임워크
commitizen==3.28.0     # Conventional Commits 검사/버전 관리

# =================================
# Security (선택)
# =================================
detect-secrets==1.5.0  # 시크릿 누출 탐지
